node_modules/
dist/
target/
.venv/
*.tmp-*
examples/*/out/
.mbse/

# Local secrets — never commit. `.env.example` (committed) is the template;
# `pnpm weave` / `pnpm weave:eval` auto-load `.env` via --env-file-if-exists.
.env
.env.local
.env.*.local
