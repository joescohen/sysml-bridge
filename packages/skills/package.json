{
  "name": "@sysml-bridge/skills",
  "version": "0.1.0",
  "private": true,
  "description": "/mbse orchestrator + verb skills (markdown contracts over the MCP tool surface)",
  "scripts": {
    "build": "echo skills: nothing to build",
    "test": "echo skills: no tests",
    "lint": "echo skills: markdown only"
  }
}
