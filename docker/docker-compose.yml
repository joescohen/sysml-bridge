# All Docker services run on the Windows desktop (10.0.0.111).
# See C:\Users\jsc61\syson\docker-compose.yml on that machine.
#
# Start tunnel from Mac:
#   autossh -N -L 8080:localhost:8080 -L 9000:localhost:9000 -L 8888:localhost:8888 winserver
#
# Ports forwarded:
#   8080 → SysON
#   9000 → sysml-api
#   8888 → Jupyter
